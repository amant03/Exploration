services:
  - type: web
    name: spring-boot-backend
    plan: free
    region: oregon
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/*.jar
    rootDir: backend